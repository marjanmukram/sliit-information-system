import React from 'react'

const Course = ({code, name}) =>{
        return(
            <div>
                <div>{code}</div>
                <div>{name}</div>
            </div>
        )
}

export default Course;